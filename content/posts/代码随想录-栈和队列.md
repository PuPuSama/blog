逆波兰表达式：
求解前的问题
int evalRPN(vector<string>& tokens) 题目的输入格式是这样的，
问题1，栈应该选用什么类型 stack<>,
假如选用char类型，如何检测入栈的元素是数字还是符号，
我有一个小思路，就是用if，如果不是四种运算符号，那就入栈，如果是的话，弹出两个元素，并对其强转？cpp 如何将字符型的数字强转成int？还有符号如何判断，只能写四个if嘛？四个if要不要分成两个支路去判断，会不会能提升点效率。
还有个问题就是除法，哪个弹出的元素是除数还有被除数。ok 开始一一写

解决思路： 栈的类型选用int，类型转换是用的stoi()函数

滑动窗口最大值：
什么是单调队列?
这道题为什么要引入单调队列？
