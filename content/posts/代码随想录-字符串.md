459.重复的子字符串
l = next[n-1] 表示整个字符串的最长相等前后缀长度。若这个长度为 l，那么后缀的起点就是 n-l，也就是说字符串每隔 q = n-l 个位置会重复一次，因此 q 是该串的最小周期长度。  
但“有周期”不等于“能由某个子串完整重复拼接而成”。只有当 n % q == 0 时，长度为 q 的周期块才能刚好铺满整个字符串，所以这时它才是真正的重复子串模式。  
因此最终判定条件是：
`l > 0 && n % (n - l) == 0`
其中 l > 0 表示至少存在非空相等前后缀，n % (n-l) == 0 表示最小周期能完整覆盖整个串。